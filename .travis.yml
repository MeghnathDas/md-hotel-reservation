os: linux

language: node_js

node_js:
   "10.14.2"

cache: npm

install:
  - npm install

script:
  - npm build

deploy:
  provider: pages
  cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  email: meghnath.babin@gmail.com
  name: Meghnath Das
  target_branch: gh-pages
  on:
    branch: master